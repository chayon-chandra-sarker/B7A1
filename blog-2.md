01. Inheritance: Inheritance allows one class to reuse the properties and methods of another class.

02. Polymorphism: Polymorphism allows the same method to behave differently in different classes.

03. Abstraction: Abstraction hides unnecessary implementation details and shows only essential functionality.

04. Encapsulation: Encapsulation protects data by restricting direct access.