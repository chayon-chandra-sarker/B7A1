In TypeScript, any is called a type safety hole because it disables TypeScript’s type checking.

The unknown type can accept any kind of data, but it forces you to perform a type check before using the value, making it a safer choice.

Type narrowing means reducing a broad type into a more specific type after checking it.